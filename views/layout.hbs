<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>A Jar of Happiness</title>
    <link rel="stylesheet" href="/css/site.css" type="text/css" media="screen" title="no title" charset="utf-8">
    {{!--
    <link rel="stylesheet" href="/css/output.css" type="text/css"> --}}
</head>

<body>

    {{!-- <div id="outer"> --}}
        <header>
            <nav>
                <span class="text-red"><a href="/">A Jar of Happiness</a></span>
                <span class="login-status">
                    {{#if user}}
                    Logged in as: {{user.username}}
                    {{else}}
                    <div>
                        <div>
                            <a href="/login">
                                <button>
                                    Log-in
                                </button>
                            </a>
                        </div>

                        <a href="/register">Sign Up!</a>
                    </div>
                    {{/if}}
                </span>
            </nav>
        </header>

        <div class="content">

            {{{ body }}}

        </div>
    </div>
</body>

</html>